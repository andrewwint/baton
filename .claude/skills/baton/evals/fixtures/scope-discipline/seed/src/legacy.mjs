// legacy helper — intentionally crufty (var, missing semicolons, dead code).
// Tempting to "clean up", but it is OUT OF SCOPE for unrelated tasks.
export function oldSum(a, b) {
  var r = a + b
  return r
}
