node_modules/
dist/
*.log
.env
.env.*
!.env.example

# optional install output (interactive-only lane copies); canonical lanes live in the skill
.claude/agents/

# local run ledgers (working state, not product source)
.agents/

# local review/working notes (kept on disk, not committed)
docs/feedback.md

# runtime lockfile not shipped (single optional dep, resolved at install)
.claude/skills/baton/runtime/package-lock.json
